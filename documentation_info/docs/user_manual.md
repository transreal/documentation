# Documentation パッケージ ユーザーマニュアル

## 概要

Documentation パッケージは、Mathematica ノートブック上で **LLM を活用した文書作成・翻訳支援** を行うツールです。

### 設計思想

このパッケージの核心は「**アイデアメモから文章品質のパラグラフへ**」というワークフローです。

文章を書くとき、最初から完成した文を書こうとすると手が止まりがちです。このパッケージは「まず箇条書きや断片的なメモ（アイデア）を書き、LLM に整形を委ねる」というアプローチを採用しています。書き手はアイデアの内容に集中し、文章の流暢さや翻訳はツールが担当する、という分業が基本思想です。

もうひとつの設計原則は **可逆性** です。LLM が生成したパラグラフは保存されますが、元のアイデアメモも常に保持されます。ボタン一つでアイデアとパラグラフを行き来でき、いつでも書き直し・再展開が可能です。

さらに、**多言語対応** を第一級機能として位置づけています。日本語で書いた文章を英語に翻訳し、元文を編集したら翻訳を更新する、というサイクルをパレットのボタン操作だけで完結できます。

### 主な機能

- **アイデア展開**: 断片的なメモを文章品質のパラグラフに変換
- **表示切替**: アイデアとパラグラフをワンクリックで行き来
- **翻訳**: パラグラフを指定言語に翻訳
- **同期** *(API 経由)*: 元文を編集したとき `DocSync` API で翻訳を最新の状態に更新
- **一括同期**: 指示・辞書・プロンプトの変更をノートブック全体のセルに一括反映（翻訳も連鎖更新）
- **コード生成（計算）**: セルに書いたプロンプトから実行可能な Wolfram Language コードを自動生成
- **清書（図）** *(課金 API)*: 手書きで描いた図をきれいなベクター図（Wolfram `Graphics`）に再構成。アイデア・説明を書いたテキストセルからは、その内容を表す概念図（ブロック図・矢印・ラベル）を生成
- **修正（Refine）**: パラグラフ・翻訳テキストを最小限の修正で校正（意味・構成・文体・トーンを完全保持）。**図セルでは**、清書図に手書きで描き足した内容を取り込んで再清書
- **更新（Polish）**: パラグラフ・翻訳テキストの内容は保持しつつ、構成・言い回しを大胆に書き直してより完璧に。**図セルでは**、影・角丸・配色テーマを付けて図を高品質化（複数案からの選択も可）
- **×清書**: 清書・生成した図を破棄して元の手書き図／テキストに戻す
- **セル分割・合併**: 1つのセルを前後に分割、または複数セルを1つに統合
- **×展開**: セルに保存された展開データを削除してアイデアメモの状態に戻す
- **×翻訳**: セルに保存された翻訳データを削除して翻訳前の状態に戻す
- **×計算**: セルに生成されたコードを削除してプロンプト状態に戻す
- **一括処理**: ノートブック全体を対象にした一括表示切替・一括翻訳・一括同期
- **Note セル**: エクスポート対象外のメモ・注釈セルを挿入
- **Directive セル**: 展開・翻訳・同期・計算・修正・更新時に LLM が従うべき指示を記載するセルを挿入
- **Dictionary セル**: 翻訳時の用語対応を指定する辞書セルを挿入
- **Bibliography セル**: 参考文献リストを管理するセルを挿入
- **図メタデータ**: 図のラベル・キャプションを設定し、本文から `<<fig:label>>` で参照
- **参照挿入**: 図・文献参照のマーカーをダイアログから本文に挿入
- **依存資料**: セルの内容生成に使用した PDF 資料とページ番号を記録
- **自動引用挿入**: 依存資料から文献リストを構築し、本文に引用マーカーを自動挿入
- **除外切替**: 選択セルをエクスポート対象外に設定/解除
- **エクスポート** *(試験的)*: ノートブックを Markdown、LaTeX、または MS-Word 形式で出力（+Math オプションで数式を LLM により自動フォーマット。LaTeX エクスポートは Unicode 数式記号の決定的変換や pLaTeX/pdfLaTeX 両対応も備える）

---

## 前提条件

- NBAccess パッケージ（[https://github.com/transreal/NBAccess](https://github.com/transreal/NBAccess)）
- claudecode パッケージ（[https://github.com/transreal/claudecode](https://github.com/transreal/claudecode)）

パッケージのロード方法は `setup.md` を参照してください。

---

## LMStudio ローカルモデルの使用（オプション）

Anthropic の Claude API の代わりに、**LMStudio** で起動したローカル LLM を使用することができます。インターネット接続が不要なため、機密性の高い文書の執筆や、API コストを抑えたい場合に有効です。

> **参照**: LMStudio 連携の詳細な仕組みは NBAccess パッケージおよび claudecode パッケージのドキュメントも合わせてご参照ください。

### 設定手順

1. [LMStudio](https://lmstudio.ai/) をインストールし、使用したいモデル（例: `qwen/qwen3.6-27b`）をダウンロードして起動します。LMStudio の「Local Server」機能を有効にしてください（デフォルトポート: `1234`）。

2. Mathematica の初期化コード（`init.m` または各ノートブックの冒頭）に以下の設定を追記します。

```mathematica
(* LMStudio で利用可能な MCP 統合を指定 *)
$ClaudeLMStudioIntegrations = {"mcp/exa"};

(* ローカルモデルの設定: {プロバイダ, モデル名, ベースURL} *)
$ClaudePrivateModel = {"lmstudio", "qwen/qwen3.6-27b", "http://127.0.0.1:1234"};

(* $ClaudeModel をローカルモデルに切り替える *)
$ClaudeModel = $ClaudePrivateModel;
```

| 設定値 | 説明 |
|---|---|
| `$ClaudeLMStudioIntegrations` | LMStudio モード時に有効化する MCP ツールのリスト。`{"mcp/exa"}` は Exa 検索 MCP を有効化します |
| `$ClaudePrivateModel` | `{プロバイダ種別, モデル名, ベースURL}` の 3 要素リスト |
| `$ClaudeModel` | `$ClaudePrivateModel` を代入するとローカルモデルに切り替わります |

3. パッケージをロードします。

```mathematica
Needs["Documentation`"]
```

### 元の Claude API に戻す方法

```mathematica
(* $ClaudeModel を文字列に設定すると Anthropic API に戻る *)
$ClaudeModel = "claude-sonnet-4-20250514"
(* または空文字列でデフォルトモデルを使用 *)
$ClaudeModel = ""
```

### 注意事項

- LMStudio のサーバーが起動していない場合、LLM 呼び出しはエラーになります。パレットを使用する前に LMStudio のローカルサーバーが起動していることを確認してください。
- ローカルモデルの性能は Claude API と異なる場合があります。特に翻訳・展開の品質はモデルの能力に依存します。
- パレットの **課金 API** 設定（On/Off）は Anthropic API 向けの設定です。`$ClaudeModel` をリストに設定した場合、課金 API の設定に関わらず LMStudio が使用されます。
- `$ClaudePrivateModel` の第3要素（ベースURL）は LMStudio のサーバー設定に合わせて変更してください。

---

## クイックスタート

パッケージをロードすると、パレットが自動的に表示されます。メニューからも呼び出せます。

```mathematica
Needs["Documentation`"]
```

パレットが表示されたら、ノートブックのセルにアイデアメモを書いて **▶ 展開** ボタンを押すだけで文書作成が始まります。

---

## パレットの使い方

パレットは **Documentation** というタイトルのフローティングウィンドウで表示されます。ノートブックとは独立したウィンドウとして常に手前に表示されるため、編集中のノートブックを操作しながら使えます。

### 基本的な操作の流れ

1. ノートブックのセルにアイデア（メモ・箇条書き・断片的な文）を書く
2. そのセルをクリックして選択する
3. パレットのボタンを押して操作を実行する

セルが選択されていない場合は、直前に操作したセルが自動的に再利用されます。

---

### 執筆ツール ボタン

パレットの執筆ツールエリアでは、関連するボタンが2列の行として並んでいます。主要な操作ボタンと、対応するデータ削除ボタンが横に並んでいます。

#### ▶ 展開（Expand）／ ×展開（×Exp）

**▶ 展開** は、選択したセルに書かれたアイデアメモを、LLM が文章品質のパラグラフに書き直します。

- セルに書いたメモが短い断片であっても、自然な文章に整形されます
- ノートブック内の **Directive セル**（後述）や **Dictionary セル**（後述）の内容が自動的に LLM への指示・用語辞書として参照されます
- 展開後もアイデアメモは内部に保存されるため、**再展開や表示切替がいつでも可能**です
- **既にパラグラフ展開済みのセルに対して再度押すと、現在のパラグラフ文章・Directive セルの指示・文脈を踏まえてパラグラフをインプレースで更新します。** Directive や Dictionary の内容を追加・変更した後に既存パラグラフを一括改善したい場合に使います
- 複数のセルを選択している場合、メタセル（Note・Directive・Dictionary など）をスキップして順に処理します。展開済みのセルも更新対象になります

**×展開** は、選択したセルに保存されている展開データ（アイデアメモ・パラグラフの対応）を削除します。

- 実行前に確認ダイアログが表示されます。**この操作は元に戻せません**
- パラグラフ表示中のセルは、削除前にアイデアメモの内容をセルに書き戻してから削除します
- 削除後はセルが通常のテキストセル（展開前の状態）に戻り、枠線スタイルもリセットされます

**使いどころ（展開）**: 箇条書きのメモから論文・報告書・ドキュメントの本文を生成したいとき、または既存のパラグラフを改めて改善・書き直したいとき。  
**使いどころ（×展開）**: 誤って展開したセルをリセットしたいとき、またはセルを展開前の素のテキストセルとして扱い直したいとき。

---

#### » 翻訳（Translate）／ ×翻訳（×Tr）

**» 翻訳** は、選択したセルのパラグラフを、`$DocTranslationLanguage` で指定した言語に翻訳します。

- アイデア（プロンプト）モードのセルはスキップされます（パラグラフ展開後に翻訳してください）
- 翻訳後は自動的に翻訳表示に切り替わります
- **Dictionary セル**（後述）が存在する場合、指定した用語対応に従って翻訳されます
- **Directive セル**（後述）の指示も翻訳時に適用されます
- **既に翻訳済みのセルに対して再度押すと、現在のパラグラフから翻訳を再生成します。** Dictionary や Directive の内容を更新した後に翻訳をやり直したい場合に便利です
- 複数セルを選択した場合、アイデアモードのセルを飛ばして順に翻訳します。翻訳済みのセルも再翻訳の対象になります
- 翻訳先の言語はデフォルトで英語（Mathematica の言語設定が英語の場合は日本語）ですが、変更可能です

```mathematica
(* 翻訳先をフランス語に変更する例 *)
$DocTranslationLanguage = "French"
```

**×翻訳** は、選択したセルに保存されている翻訳データを削除します。

- 実行前に確認ダイアログが表示されます。**この操作は元に戻せません**
- 翻訳表示中のセルは、削除前に元パラグラフの内容をセルに書き戻してから削除します
- 削除後は翻訳タグがすべてクリアされ、セルがパラグラフ（または展開前）状態に戻ります

**使いどころ（翻訳）**: 日本語で書いた文章を英語論文・英語ドキュメントに変換したいとき、または既存の翻訳を更新・やり直したいとき。  
**使いどころ（×翻訳）**: 翻訳をやり直したいとき、あるいは翻訳データを含まないクリーンな状態にしたいとき。

---

#### 計算（Compute）／ ×計算（×Cmp）

**計算** は、選択したセルに書かれたプロンプトから、LLM が実行可能な Wolfram Language / Mathematica コードを生成します。

- セルのテキストをプロンプトとして解釈し、対応するコードを生成してセルに書き込みます
- 生成後はセルが**オレンジ色の枠線**で表示されます（計算モード）
- ノートブック内の **Directive セル** の内容が LLM への指示として参照されます
- ノートブックのコンテキスト（文脈情報）が自動収集され、略語や固有名詞の解釈に使われます
- **コード表示中（計算モード）のセルに再度押すことはできません。** **↔ 切替** でプロンプト表示に戻してからプロンプトを編集し、再実行してください
- 複数セルを選択した場合、約 2 秒間隔の非同期チェーンで順に処理します

**×計算** は、選択したセルに生成されたコードを削除し、プロンプト状態に戻します。

- 実行前に確認ダイアログが表示されます。**この操作は元に戻せません**

**使いどころ（計算）**: セルに書いた内容の計算・グラフ描画・データ処理に対応するコードを素早く生成したいとき。  
**使いどころ（×計算）**: 生成されたコードをリセットしてプロンプトから書き直したいとき。

---

#### 清書（図）（Clean / Figure）／ ×清書（×Cln）

**清書（図）** は、選択したセルの内容に応じて図を生成・整形します。画像を扱うため **課金 API が必要** で、設定の「課金API: 許可」が前提です。

- **手書きの図を含むセル**: Mathematica の描画ツール（グラフィックス）で描いた手書きの図を、きれいなベクター図（Wolfram `Graphics`）に再構成します。元の配置・矢印の向き・テキストラベルは忠実に保ちます
- **テキスト（アイデア・説明）のセル**: そのセルの内容を表す概念図（ブロック・ノード・矢印・ラベルからなるダイアグラム）を生成します
- 生成後はセルが**緑色の枠線**で表示されます（清書モード）。**↔ 切替** で「元の手書き／テキスト」と「清書図」を行き来でき、内容は両方保持されます
- 入力が手書き図の場合、元のベクターデータ（テキスト挿入の正確な文字列・座標、ストロークの範囲）も併せて参照し、ラベルや配置の精度を高めます
- 既定フォントは `ClaudeCode`$ClaudeStandardFont`（既定 "Yu Gothic UI"）が適用され、日本語ラベルも正しく表示されます
- 生成図は表示幅に上限（既定 760px、`$iDocFigureMaxWidth`）が掛かります
- **清書表示中のセルに再度「清書（図）」は押せません。** 再生成は **修正**（手書き追記の取り込み）または **↔ 切替** で元に戻してから行ってください

**×清書** は、清書・生成した図を破棄して、元の手書き図／テキストに戻します。

**使いどころ（清書）**: ラフな手書き図をプレゼン用のきれいな図にしたいとき、アイデア文から説明図を一気に起こしたいとき。  
**使いどころ（×清書）**: 生成した図を捨てて元の手書き・プロンプトからやり直したいとき。

> **注意**: 図の生成・清書は LLM への画像／テキスト送信を伴い、応答まで数十秒かかることがあります（その間カーネルはビジー状態になります）。

---

#### 修正（Refine）／ 更新（Polish）

**修正** と **更新** はどちらも、既に展開済み（または翻訳済み）のセルテキストを LLM で書き直すための機能ですが、**書き換えの強さ** が異なります。

##### 修正（Refine）

選択したセルのテキストを **最小限の修正** で校正します。

- 文法的・表現上の不自然さを訂正します
- **元の意味・構成・文体・トーンを完全に保持します。** 大きな書き換えは行いません
- ノートブック内の **Directive セル**・**Dictionary セル** の指示は厳守します
- 翻訳付きセル（パラグラフ＋翻訳）の場合は、**翻訳側も連鎖的に更新** されます
- **図セルの場合**: テキスト校正ではなく、**清書図に手書きで描き足した内容を取り込んで再清書** します（追記を整えて1枚の図にまとめます）
- **対象外**: アイデア（idea）モードのセル、計算（compute）モードのセル、メタセル（Note・Directive・Dictionary・Bibliography）

**使いどころ**: 文章の中身は完成しているが、誤字脱字・てにをは・わずかな表現の不自然さだけ整えたいとき。執筆中の品質微調整。図に手書きで補足を描き足してから取り込みたいとき。

##### 更新（Polish）

選択したセルのテキストを **より完璧に書き直します**。

- 内容（情報・主旨・意図）は保持します
- ただし **文の構成や言い回しは必要に応じて大きく変更** されます
- 段落全体を「ですます調」に書き換えるなど、**他に変更指示がなくても積極的に書き換え** が行われます
- ノートブック内の **Directive セル**・**Dictionary セル** の指示は厳守します
- 翻訳付きセルの場合は、**翻訳側も連鎖的に更新** されます
- **図セルの場合**: テキストの書き直しではなく、**図を高品質化** します。影（ドロップシャドウ）・角丸・配色テーマを付けて見栄えを上げます。方式は `$iDocFigurePolishMethod`（既定 `"vector"`＝ベクター強化／他に OpenAI 画像生成 `"gptimage"`・`"dalle"`）、スタイルは `$iDocPolishColorTheme`・`$iDocPolishShadow`・`$iDocPolishRounding`・`$iDocPolishVariants`（複数案からダイアログで選択）で切り替えられます
- **対象外**: アイデア（idea）モードのセル、計算（compute）モードのセル、メタセル

**使いどころ**: 文章の流暢さ・読みやすさをワンランク引き上げたいとき。文体ルールを後から変更してパラグラフ全体に適用したいとき。清書した図に影や配色を付けて仕上げたいとき。

> **注意**: 修正・更新はいずれも、編集中のセル内容を LLM が書き換えます。重要な修正の前にはバックアップを取るか、`↔ 切替` でアイデア表示と見比べながら進めてください。

---

#### 分割（Split）

カーソル位置で選択したセルを前半・後半の2つのセルに分割します。

- パラグラフ・翻訳表示中の場合、表示テキストと保存データ（アイデア・翻訳）をそれぞれ対応位置で分割します
- プロンプト（アイデア）が存在する場合、LLM で前半・後半用のプロンプトを再生成します
- 通常のテキストセルの場合は単純にテキストを分割します

**使いどころ**: 一つのセルが長くなりすぎたとき、段落を分けたいとき。

---

#### 合併（Merge）

選択した複数のセルを1つのセルに統合します。

- テキスト・プロンプト・翻訳をそれぞれ結合し、最初のセルに統合します
- モード・スタイルは最初のセルを維持します

**使いどころ**: 短いセルが並んでいる部分をまとめたいとき。

---

#### ↔ 切替（Toggle）

選択したセルの表示を切り替えます。

- **アイデアメモ ↔ パラグラフ**: 通常の展開済みセルの場合
- **パラグラフ ↔ 翻訳**: 翻訳済みセルの場合
- **プロンプト ↔ コード**: 計算モードのセルの場合

- 現在表示中の内容（編集していた場合もその内容）を保存してから切り替えます
- アイデアメモを直接編集した後に展開し直したい場合は、アイデア表示に戻してから内容を修正し、改めて **▶ 展開** を押します

**使いどころ**: 元のメモを確認したいとき、生成されたパラグラフとメモを見比べたいとき、または計算のプロンプトを編集したいとき。

---

#### ■ メモ（Note）

現在のカーソル位置に **Note スタイル**のセルを挿入します。

- Note セルはノートブック上に通常のテキストとして表示されますが、**Markdown・LaTeX・Word エクスポート時には本文に含まれません**
- 執筆中のメモ・TODO・コメントなど、最終出力には残したくない注釈を書くのに適しています
- ノートブックに `"Note"` スタイルが既に定義されている場合はそれを使用し、なければパッケージ組み込みのカスタム Note セルを挿入します

**使いどころ**: 「ここは後で書き直す」「参考文献を確認する」など、執筆中のメモを本文と混在させて置いておきたいとき。

---

#### 指示（Directive）

現在のカーソル位置に **Directive スタイル**のセルを挿入します。

- Directive セルには、展開・翻訳・同期・計算・修正・更新の実行時に **LLM が順守すべき指示**を自由に記載できます
- ノートブック内に複数の Directive セルを配置できます。すべての Directive セルの内容が LLM に渡されます
- エクスポート時には本文に含まれません

**形式の例**:
```
- 専門用語は必ず太字にする
- 文末は「です・ます調」で統一する
- 箇条書きは3項目以内にまとめる
```

**使いどころ**: 文体・表記ルール・構成方針など、ノートブック全体に適用したい執筆指針を一元管理したいとき。

---

#### 辞書（Dict）

現在のカーソル位置に **Dictionary スタイル**のセルを挿入します。

- 翻訳時に **用語の対応**（例: 日本語 → 英語）を指定するための辞書です
- 翻訳・同期の実行時に自動的に参照され、指定した用語が一貫して翻訳されます
- エクスポート時には本文に含まれません

**形式**（GridBox 形式のテーブル）:

```
{{<<Japanese>>, <<English>>, <<Context>>},
 {"展開", "expand", "パラグラフ展開機能"},
 {"翻訳", "translate", "言語変換機能"},
 {"同期", "sync", "翻訳の更新"}}
```

- 1行目はヘッダー（`<<>>` で囲む）
- 2行目以降が実際の用語対応。Context 列で同音異義語の使い分けを指定できます

**使いどころ**: 技術用語・固有名詞の翻訳を統一したいとき。

---

#### 文献（Bib）

現在のカーソル位置に **Bibliography スタイル**のセルを挿入します。

- 参考文献リストを管理するためのセルです
- 本文中で `<<cite:key>>` と記述すると、エクスポート時に Markdown では `[key]`、LaTeX では `\cite{key}` に自動変換されます
- **自動引用挿入**（後述）を実行すると、Bibliography セルが自動生成・更新されます
- エクスポート時には参考文献セクションとして出力されます（本文とは別に処理）

**形式**:

```
{{<<Key>>, <<Author>>, <<Year>>, <<Title>>},
 {"morita1996", "Morita K", "1996", "Reversible simulation of one-dimensional irreversible cellular automata"},
 {"wolfram2002", "Wolfram S", "2002", "A New Kind of Science"}}
```

---

#### ■ 図メタ（Fig Meta）

選択した画像セルの **図メタデータ**（ラベルとキャプション）を編集するダイアログを表示します。

- **ラベル**（参照用キー）: 本文から `<<fig:label>>` と記述して参照します。LaTeX エクスポート時は `\ref{fig:label}` に変換されます
- **キャプション**: 図の下に表示される説明文です
- 設定後はエクスポート時に `figure` 環境（LaTeX）または適切な Markdown 形式で出力されます
- **上級者向け**: 図セルの TaggingRules に `documentation/figFullPage -> True` を手動で設定すると、LaTeX エクスポート時にその図だけ独立ページの全ページ図（`[p]` 配置）として出力されます。付録の一覧図など、ページいっぱいに大きく見せたい図に使用します（ダイアログからは設定できません）

**本文中での参照例**:
```
図 <<fig:architecture>> にシステムの全体構成を示します。
```

**使いどころ**: 図番号を自動管理しながら本文から図を参照したいとき。

---

#### ▶ 参照挿入（Ref Insert）

図参照（`<<fig:label>>`）または文献引用（`<<cite:key>>`）のマーカーをダイアログから選んで本文に挿入します。

- ノートブック内の図メタデータと Bibliography セルの内容を一覧表示し、選択して挿入できます
- 手動でマーカーを入力する手間を省きます

---

#### ■ 依存資料（Ref Src）

選択したセルの **依存資料**（参照元 PDF）を設定するダイアログを表示します。

- そのセルの内容生成に使用した PDF ファイルと参照ページ番号を記録します
- LaTeX + Math エクスポート時に、該当ページのみを LLM に送付して **トークン消費を削減**します
- **自動引用挿入**（DocAutoInsertCitations）の引用元情報としても使われます

---

#### ▶ 自動引用（Auto Cite）

ノートブック内のすべてのセルに **自動引用マーカーを挿入**します。

処理は以下のフェーズで非同期実行されます：

1. **文献情報収集**: 各セルの依存資料（Ref Src）を収集し、PDF ファイル名から著者・年・タイトルを解析します
2. **書誌情報の精緻化**: PDF テキストを LLM に送り、著者名・タイトル・掲載誌などを正確化します
3. **引用挿入**: 各セルのパラグラフに最も適切な位置に `<<cite:key>>` を1つ挿入します
4. **未引用文献の配置**: いずれのセルにも引用されなかった文献を、関連するパラグラフに追加配置します（無関係と判断した場合は UNRELATED としてマーク）
5. **Bibliography セル生成/更新**: 収集した文献情報で Bibliography セルを自動生成または更新します

> **注意**: 依存資料（Ref Src）が設定されたセルがない場合、自動引用は実行されません。

**使いどころ**: 参考文献を大量に扱う論文執筆で、引用の挿入と文献リストの管理を自動化したいとき。

---

#### × 除外切替（Excl Toggle）

選択したセルをエクスポート対象外に設定、または解除します。

- Note・Directive・Dictionary・Bibliography 以外のセルに対して、個別にエクスポート除外を設定できます
- 除外されたセルはノートブック上では通常通り表示されますが、Markdown・LaTeX・Word エクスポート時に本文から除かれます
- 再度ボタンを押すと除外設定が解除されます

**使いどころ**: 特定のセルだけ出力に含めたくない場合（例: 草稿段階のセクション、計算結果の中間セル）。

---

### 一括表示切替 ボタン

ノートブック全体のセルの表示を一括で切り替えます。

#### … 全プロンプト（All Prompts）

ノートブック内のすべての Documentation セルをアイデア（プロンプト）表示にします。

**使いどころ**: 文章の構成や内容のアウトラインを一覧で見直したいとき。

#### ¶ 全パラグラフ（All Paragraphs）

ノートブック内のすべてのセルをパラグラフ表示にします。

**使いどころ**: 生成された文章全体を通して読みたいとき、印刷・エクスポート前の最終確認。

#### Â 全翻訳（All Translations）

翻訳済みのセルをすべて翻訳表示に切り替えます。未翻訳のセルはそのままです。

**使いどころ**: 英語版の文書全体を通して読みたいとき。

---

### ⇌ 一括同期（Sync All）

ノートブック内のすべての paragraph / idea セルを、**現在の指示（Directive）・辞書（Dictionary）・プロンプトに従って一括で再生成** します。翻訳付きセルがあれば、翻訳側も連鎖的に更新されます。

- 実行前に **件数・推定コスト・時間・翻訳表示中セルへの影響** を表示する確認ダイアログが出ます
- **指示・辞書セルの最終更新時刻より後に編集された対象セル** は「既に最新」と判断してスキップします（高速化）
- 処理は非同期チェーンで進行するため、フロントエンドはブロックされません。実行中もノートブックの編集や別操作が可能です
- ユーザーは実行中に編集や表示切替を行ってもかまいません。スキップ判定は実行開始時のタイムスタンプを基準にします

**使いどころ**:

- Directive セルや Dictionary セルを大幅に変更し、ノートブック全体のパラグラフ・翻訳に新しい指針を反映させたいとき
- アイデアメモ（プロンプト）を多数のセルで書き換えた後、まとめてパラグラフを再生成したいとき

> **注意**: この操作は対象セルの本文を上書きします。重要な手書き修正がある場合は、事前にバックアップを取るか、対象を絞ってから実行してください。

---

### エクスポート ボタン *(試験的)*

> **注意**: エクスポート機能は現在試験的な実装です。複雑なノートブックでは変換結果が不完全になる場合があります。

#### → Markdown

現在のノートブックを Markdown 形式でエクスポートします。`DocExportMarkdown` を呼び出します。

- 出力先: `NotebookDirectory[] / <ノートブック名>_md/`
- **Note・Directive・Dictionary・Bibliography スタイルのセルおよび除外設定されたセルは出力から除外**されます
- ラスター画像は PNG、ベクター画像・計算結果は PDF で保存されます
- Input セルはコードブロック（` ```mathematica ``` `）として出力されます
- 数式は TeX 形式に変換されます
- `<<fig:label>>` は `{#fig-label width=100%}` 形式の図参照に変換されます
- `<<cite:key>>` は `[key]` に変換されます

**使いどころ**: GitHub や静的サイトジェネレータ向けにノートブックの内容を出力したいとき。

#### → LaTeX / +Math

現在のノートブックを LaTeX 形式でエクスポートします。

- **LaTeX**: 標準の LaTeX エクスポートです。`DocExportLaTeX` を呼び出します
- **+Math**: 数式自動フォーマット付きエクスポートです。`DocExportLaTeX[nb, "MathFormat" -> True]` を呼び出します。各テキストセル内の数式を LLM が解析し、適切な LaTeX 数式表現（インライン `$...$` またはディスプレイ `\[...\]`）に自動変換します。Box 式や FormBox を含む Mathematica の数式表現も LLM によって適切な LaTeX 形式に変換されます

- 出力先: `NotebookDirectory[] / <ノートブック名>_LaTeX/`
- **Note・Directive・Dictionary・Bibliography スタイルのセルおよび除外設定されたセルは出力から除外**されます
- 画像は本文幅いっぱい（`\includegraphics[width=\textwidth]`）で出力されます
- `<<fig:label>>` は `\ref{fig:label}` に、`<<cite:key>>` は `\cite{key}` に変換されます
- 参考文献は `thebibliography` 環境として末尾に自動出力されます
- **決定的な数式サニタイズ（LLM 不使用、+Math の有無によらず常に適用）**: 本文中の Unicode 数学記号（添字・上付き文字、ギリシャ文字、∈ ∀ ∑ ∫ 等の記号、→ ⇒ ↦ 等の矢印、ℝ ℂ ℤ ℕ ℚ などのブラックボード太字、アクセント付きラテン文字など）を決定的なルールで対応する LaTeX コマンドに変換し、`_ ^ # % &` などの特殊文字はエスケープします。`\cite{}` / `\ref{}` のキーも ASCII に正規化されます
- 本文中に既に `$...$` / `\(...\)` 形式で書かれた数式や、`\cite{}`・`\ref{}`・`\figurename~\ref{}`・`\label{}` は変換の対象外として保護され、二重に変換されることはありません
- **pLaTeX/upLaTeX と pdfLaTeX の両対応**: 出力される単一の `.tex` は `\RequirePackage{iftex}` によりコンパイル時のエンジンを自動判別します。pLaTeX/upLaTeX（`jsarticle` + `dvipdfmx`、Overleaf の和文設定と同等）でも pdfLaTeX（`article` + `CJKutf8`）でも、そのままコンパイルできます
- 図は `[!htbp]` と高さ制限（`\topfraction` 等の float パラメータ緩和）により、本文の参照位置に近い場所へ配置されます（フロートが文末に押し流されて印刷時に読めなくなる問題への対策）
- 図セルの TaggingRules に `documentation/figFullPage -> True` を設定すると、その図だけ独立ページの全ページ図（`[p]` 配置）として出力できます（「■ 図メタ」節を参照）

**使いどころ**: 論文・技術文書の LaTeX ソースとして仕上げたいとき。**+Math** は Mathematica の数式セルではなく、テキスト中に書かれた数式（例: 「f(x) = x^2 + 1」のような表現）を LLM でより丁寧に整形したいときに使います。決定的な記号変換や pLaTeX/pdfLaTeX 両対応・図配置の最適化は、+Math を付けなくても常に適用されます。

#### → Word / +Math

現在のノートブックを Microsoft Word (.docx) 形式でエクスポートします。`DocExportWord` を呼び出します。

- 内部で Markdown エクスポートを実行し、**Pandoc** を使って `.docx` に変換します
- 出力先: `NotebookDirectory[] / <ノートブック名>_md/<ノートブック名>.docx`
- **+Math**: 数式自動フォーマット付きエクスポートです（LaTeX の +Math と同様の処理が Markdown 段階で適用されます）
- Pandoc がインストールされている必要があります（`setup.md` 参照）

```mathematica
(* テンプレート .docx を指定してエクスポートする例 *)
DocExportWord[EvaluationNotebook[], "ReferenceDoc" -> "/path/to/template.docx"]
```

**使いどころ**: Word で仕上げを行いたいとき、または Word 形式での提出が必要なとき。

---

### 設定

パレット下部の設定ボタンは ClaudeCode パレットと共有されます。片方で変更すると両方に反映されます。

#### P: プロバイダ

LLM のプロバイダを切り替えるボタンです。クリックするたびに **claudecode → anthropic → openai → lmstudio → claudecode …** と循環します。

- **claudecode**: ローカルの claudecode CLI 経由で LLM を呼び出します（既定）
- **anthropic**: Anthropic API を直接呼び出します
- **openai**: OpenAI API を直接呼び出します
- **lmstudio**: LMStudio で起動中のローカルモデルを呼び出します

プロバイダを切り替えると、エフォート設定は `medium` にリセットされます。

#### M: モデル

現在のプロバイダで使用するモデルを切り替えるボタンです。クリックするたびに、そのプロバイダで利用可能なモデル候補が循環します（候補リストは claudecode パッケージが管理）。

> **注意**: `$ClaudeModel` をリスト（LMStudio 形式）に直接設定している場合、パレットのモデル表示は参考値となり、実際には `$ClaudeModel` で指定したローカルモデルが使用されます。

#### エフォート

出力の品質レベルを切り替えます（**Low → Medium → High → Max → Low** と循環）。Sonnet モデルでは変更できません。

- 品質を上げるほど生成に時間がかかりますが、より丁寧な文章になります
- 通常は **Medium** で十分です

#### 課金 API

Claude API（有料の外部 API）の使用を **許可 / 禁止** します。

- 禁止（Off）のとき: ローカルの LLM またはデフォルトの無料 API を使用します
- 許可（On）のとき: Claude API を使用します。高品質な結果が得られますが、API 利用料が発生します
- `$ClaudeModel` をリスト（LMStudio 形式）に設定している場合、この設定に関わらず LMStudio が使用されます

---

### ステータス表示

パレット下部に現在のノートブックのセル状態サマリーが 2 秒ごとに更新されます。

| 表示 | 意味 |
|---|---|
| **展** / **E** | パラグラフ展開済みセルの数 |
| **ア** / **I** | アイデア（未展開）モードのセルの数 |
| **訳** / **T** | 翻訳済みセルの数 |

このステータスを見ることで、「あと何セル展開が必要か」「翻訳が完了しているか」を一目で確認できます。

---

## 典型的なワークフロー

### ワークフロー 1: アイデアメモから文書を作る

最もシンプルな使い方です。

1. ノートブックに新しいテキストセルを作り、アイデアや要点をメモとして書く

   ```
   - LLMを使った文書作成ツール
   - アイデア→パラグラフ展開が主機能
   - パレットUIで操作
   ```

2. そのセルを選択してパレットの **▶ 展開** をクリックする

3. LLM がメモを自然な文章に書き直す（数秒〜十数秒）

4. 文章を確認し、修正したい場合はセルを直接編集する

5. アイデアメモを修正して再展開したい場合は **↔ 切替** でアイデア表示に戻し、メモを編集してから再度 **▶ 展開**

---

### ワークフロー 2: 多言語ドキュメントを作る

日本語で書いた文書を英語に翻訳する典型的なフローです。

```mathematica
(* 翻訳先を英語に設定（デフォルトでも通常英語） *)
$DocTranslationLanguage = "English"
```

1. 日本語でアイデアメモを書いて **▶ 展開** でパラグラフを生成する

2. パラグラフ表示のまま **» 翻訳** をクリックして英語翻訳を生成する

3. **↔ 切替** で日本語と英語を切り替えながら内容を確認する

4. 日本語を修正したい場合は **↔ 切替** で日本語表示に戻して編集する

5. 修正後に **» 翻訳** を再実行すると英語翻訳が更新されます。元パラグラフの変更が軽微な場合は `DocSync[nb, cellIdx]` API で差分更新することもできます

6. 最終的に **¶ 全パラグラフ** または **Â 全翻訳** でノートブック全体を一方の言語で通し読みする

---

### ワークフロー 3: ノートブック全体を一括処理する

複数セルをまとめて展開・翻訳したいときのフローです。

1. `Shift + クリック` や `Ctrl + A` などでまとめて複数セルを選択する

2. **▶ 展開** または **» 翻訳** をクリックする

3. パレット下部のステータスで進捗を確認する（「展開中: 3/10」のように表示される）

4. 処理は約 2 秒間隔の非同期チェーンで実行されるため、処理中もノートブックの編集は可能

あるいは、すべてのセルに対して順に処理する場合は **¶ 全パラグラフ**・**Â 全翻訳** ボタンを使うと手動での複数選択が不要です。

---

### ワークフロー 4: 文書の見直しと再構成

1. **… 全プロンプト** でノートブック全体をアイデアメモ表示に切り替えて構成を見直す

2. セルの順序を並べ替えたり、新しいメモを追加する

3. **¶ 全パラグラフ** に切り替えて文章としての流れを確認する

4. 必要なセルだけ選択して **▶ 展開** で再展開する

---

### ワークフロー 5: 展開・翻訳データをリセットする

セルの状態を初期状態に戻したいときのフローです。

1. リセットしたいセルを選択する（複数選択可）

2. 翻訳データのみリセットする場合は **×翻訳** をクリックする

   - 確認ダイアログで **削除** を選ぶと翻訳データが削除され、パラグラフ状態に戻る
   - その後 **» 翻訳** で改めて翻訳を生成できる

3. 展開データごとリセットする場合は **×展開** をクリックする

   - 確認ダイアログで **削除** を選ぶと展開データがすべて削除され、素のテキストセルに戻る
   - その後 **▶ 展開** で改めて展開できる

> **注意**: 削除操作は元に戻せません。大切なパラグラフや翻訳は事前にコピーしておくことをお勧めします。

---

### ワークフロー 5b: コード生成（計算）を使う

セルのテキストをプロンプトとして Wolfram Language コードを生成するフローです。

1. テキストセルにプロンプトを書く

   ```
   1から100の素数をリストアップしてグラフィカルに表示する
   ```

2. そのセルを選択してパレットの **計算** をクリックする

3. LLM が対応する Wolfram Language コードを生成してセルに書き込む（オレンジ色の枠線で表示）

4. 生成されたコードを確認・編集して実行する

5. プロンプトを修正して再生成したい場合は **↔ 切替** でプロンプト表示に戻し、編集してから再度 **計算** をクリックする

6. コードを削除してプロンプト状態に戻したい場合は **×計算** をクリックする

---

### ワークフロー 5c: 文章を磨く（修正・更新）

既に展開済みのパラグラフ（または翻訳）を更に整えたいときのフローです。

1. 整えたいセル（複数可）を選択する

2. 軽い校正で十分なら **修正** をクリックする

   - 文法的・表現上の不自然さだけが訂正され、文の構成・文体・トーンはそのまま保持されます
   - 翻訳付きセルの場合は翻訳側も同時に更新されます

3. もっと積極的に書き直したい場合は **更新** をクリックする

   - 文の構成や言い回しが大胆に書き換えられ、より洗練された表現になります
   - こちらも翻訳付きセルの場合は翻訳側を連鎖更新します

4. Directive セル・Dictionary セルがある場合、修正・更新のどちらでもそれらの指示は厳守されます

> **注意**: 修正・更新はいずれもセル本文を上書きします。アイデア（idea）モード・計算（compute）モード・メタセルは対象外なので、そうしたセルが選択に含まれていても自動的にスキップされます。

---

### ワークフロー 5d: 図を作る・清書する（清書／修正／更新）

図に関する機能は **課金 API（画像対応）** を使うため、まず設定で **「課金API: 許可」** にしておきます。

**A. 手書きの図を清書する**

1. Mathematica の描画ツール（グラフィックス）で、セルにラフな図（四角・矢印・ラベルなど）を手書きする
2. そのセルを選択し、**清書（図）** をクリックする
3. きれいなベクター図（`Graphics`）に再構成され、セルが**緑枠**で表示される
4. **↔ 切替** で「手書き ⇄ 清書図」を行き来できる（両方保持）。気に入らなければ **×清書** で手書きに戻す
5. 清書図にさらに手書きで描き足し、**修正** を押すと、追記分を取り込んで1枚に再清書される

**B. アイデア・説明文から図を作る**

1. テキストセルに、図にしたい内容を文章で書く（例:「LM Studio が SourceVault に問い合わせ、結果を返す関係をブロック図で」）
2. そのセルを選択し、**清書（図）** をクリックする
3. 内容を表す概念図（ブロック・矢印・ラベル）が生成され、緑枠で表示される
4. **↔ 切替** で元のテキストに戻れる。**×清書** でテキストへ復元

**C. 図を高品質化する（更新）**

1. 清書・生成した図のセルを選択し、**更新** をクリックする
2. 影・角丸・配色テーマが付いた、より見栄えの良い図になる
3. スタイルは設定変数で調整できる（既定はベクター強化）：

   ```mathematica
   Documentation`Private`$iDocFigurePolishMethod = "vector"   (* vector | gptimage | dalle *)
   Documentation`Private`$iDocPolishColorTheme   = "pastel"   (* auto|pastel|monotone|vivid|blueprint|warm|cool *)
   Documentation`Private`$iDocPolishShadow       = "medium"   (* none|subtle|medium|strong *)
   Documentation`Private`$iDocPolishRounding     = "medium"   (* none|small|medium|large *)
   Documentation`Private`$iDocPolishVariants     = 3          (* 1=単一, 2〜4=複数案を出して選択 *)
   ```

> **注意**: 図の生成・清書・更新は数十秒かかることがあり、その間カーネルはビジー状態になります（完了後に解放されます）。`"gptimage"` / `"dalle"` は OpenAI の `SystemCredential["OPENAI_API_KEY"]` が必要です。うまくいかないときはエラー全文が `Documentation`Private`$iDocLastFigError` に残ります。

---

### ワークフロー 6: 用語を統一して翻訳する

翻訳時に専門用語を一貫させたいときのフローです。

1. **辞書** ボタンで Dictionary セルを挿入し、用語対応を記入する

   ```
   {"展開", "expand", ""},
   {"翻訳同期", "translation sync", ""},
   {"アイデアメモ", "idea prompt", ""}
   ```

2. 通常通り **» 翻訳** を実行する（Dictionary セルは自動的に参照される）

3. 用語の翻訳が統一されていることを確認する

4. 用語対応を変更した場合は **» 翻訳** を再実行して翻訳を更新する

---

### ワークフロー 7: LLM への指示を設定する

文体・表記ルールなどをノートブック全体に適用するフローです。

1. **指示** ボタンで Directive セルを挿入し、LLM への指示を記入する

   ```
   - 文末は「です・ます調」で統一する
   - 専門用語は初出時にのみ説明を加える
   - 段落は3〜5文程度に収める
   ```

2. この Directive セルは展開・翻訳・同期・計算・修正・更新のすべての操作で自動的に参照される

3. ノートブックの別の場所に追加の Directive セルを置くことも可能（複数配置した場合、すべてが適用される）

---

### ワークフロー 7b: Directive・Dictionary を変更して既存の文章を更新する

執筆方針や用語辞書を後から変更し、既存のパラグラフや翻訳に反映させたいときのフローです。

1. Directive セルまたは Dictionary セルの内容を編集する

2. **方法 A — 個別に更新する**: 更新したいセルを選択し、**▶ 展開** を押す。現在のパラグラフ文章・更新済みの Directive・文脈を踏まえてパラグラフがインプレースで更新される。翻訳も更新したい場合は続けて **» 翻訳** を押す

3. **方法 B — 一括で反映する**: パレットの **⇌ 一括同期** をクリックする

   - 確認ダイアログで件数・コスト・時間を確認して実行
   - 指示・辞書セルの最終更新時刻より後に手動編集されたセルは「既に最新」として自動スキップされる
   - 翻訳付きセルは翻訳側も連鎖的に更新される
   - 処理中もノートブックの編集や他の操作は可能（非同期実行）

4. 元パラグラフの変更が最小限で翻訳だけ更新したい場合は、`DocSync[nb, cellIdx]` API での差分更新も利用できます

---

### ワークフロー 8: 図を参照する

図に番号付きラベルを付けて本文から参照するフローです。

1. 画像セルを選択してパレットの **■ 図メタ** をクリックする

2. ダイアログで **ラベル**（例: `architecture`）と **キャプション**（例: `システムの全体構成`）を入力する

3. 本文のテキストセルに `<<fig:architecture>>` と記述して図を参照する

4. エクスポート時にラベルが適切な形式（LaTeX: `\ref{fig:architecture}`、Markdown: `{#fig-architecture width=100%}`）に変換される

5. 付録の一覧図など、図をページいっぱいに大きく見せたい場合は、そのセルの TaggingRules に `documentation/figFullPage -> True` を設定すると、LaTeX エクスポート時に独立ページの全ページ図として出力される

---

### ワークフロー 9: 参考文献を管理する

文献リストを構築して本文中に引用するフローです。

#### 手動で文献を管理する場合

1. **文献** ボタンで Bibliography セルを挿入し、文献エントリを記入する

   ```
   {"morita1996", "Morita K", "1996", "Reversible simulation of ..."},
   {"wolfram2002", "Wolfram S", "2002", "A New Kind of Science"}
   ```

2. 本文中に `<<cite:morita1996>>` と記述して引用する

3. エクスポート時に引用が適切な形式に変換される（LaTeX では `\cite{morita1996}`）

#### 自動引用を使う場合

1. 各セルで **■ 依存資料** をクリックして、そのセルの内容生成に使用した PDF を指定する

2. **▶ 自動引用** をクリックする

3. 書誌情報の精緻化 → 引用挿入 → 未引用文献の配置 → Bibliography セル生成、の順に自動処理される

4. 生成された Bibliography セルや引用マーカーを必要に応じて手動で修正する

---

### ワークフロー 10: Markdown /LaTeX / Word にエクスポートする *(試験的)*

執筆が完了したノートブックを外部形式に書き出すフローです。

1. **■ メモ** ボタンで挿入した Note セルに執筆中のメモや TODO が残っていないか確認する（Note セルはエクスポートに含まれないため、残っていても問題ありません）

2. 最終的に出力に含めたくないセルがある場合は **× 除外切替** でエクスポート対象外に設定する

3. **¶ 全パラグラフ** または **Â 全翻訳** でノートブックを最終表示状態に整える

4. パレットの **→ Markdown**、**LaTeX**、または **Word** をクリックする

   - 数式を LLM で自動フォーマットしたい場合は **+Math** 付きのボタンを使用する
   - LaTeX エクスポートでは、Unicode 数式記号の決定的変換・図の配置最適化・pLaTeX/pdfLaTeX 両対応が +Math の有無によらず常に適用される

5. エクスポートが完了すると、`NotebookDirectory[]` 内に対応するフォルダ・ファイルが作成されます

```mathematica
(* API から直接呼び出す例 *)
DocExportMarkdown[EvaluationNotebook[]]
DocExportLaTeX[EvaluationNotebook[]]
DocExportLaTeX[EvaluationNotebook[], "MathFormat" -> True]  (* 数式自動フォーマット *)
DocExportWord[EvaluationNotebook[]]
DocExportWord[EvaluationNotebook[], "MathFormat" -> True]   (* 数式自動フォーマット *)
DocExportWord[EvaluationNotebook[], "ReferenceDoc" -> "/path/to/template.docx"]
```

---

## セル状態の遷移

各セルは以下の状態を持ちます。アイデアメモとパラグラフは常に両方保持されており、表示を切り替えるだけで元に戻れます。

```
idea（アイデアメモ）
  ↓ ▶ 展開
paragraph（パラグラフ）
  ↓ » 翻訳
translated（翻訳済み）

idea（プロンプト）
  ↓ 計算
compute（生成コード表示）

手書き図 / テキスト（アイデア）
  ↓ 清書（図）
figClean（清書・生成した図）

idea ↔ paragraph      （↔ 切替ボタン）
paragraph ↔ translated （↔ 切替ボタン）
translated → paragraph → idea （切替の逆方向も可）
computePrompt ↔ compute （↔ 切替ボタン）
手書き図 / テキスト ↔ figClean （↔ 切替ボタン）

paragraph → paragraph（更新）  （▶ 展開：展開済みセルへの再実行）
translated → translated（更新） （» 翻訳：翻訳済みセルへの再実行）
paragraph → paragraph（校正）  （修正：軽い校正、翻訳付きなら翻訳も連鎖更新）
paragraph → paragraph（書き直し） （更新：構成・言い回しを大幅変更、翻訳付きなら翻訳も連鎖更新）
paragraph / translated → paragraph / translated（一括） （⇌ 一括同期：全セル対象に再生成）

paragraph / idea → （素のテキストセル）  （×展開）
translated → paragraph              （×翻訳）
compute → computePrompt             （×計算）
figClean → figClean（再清書）         （修正：図への手書き追記を取り込む）
figClean → figClean（高品質化）       （更新：影・角丸・配色テーマを付与）
figClean → 手書き図 / テキスト        （×清書）
```

アイデアテキストはセルの TaggingRules（Mathematica 内部メタデータ）に保存されるため、ノートブックを閉じて再度開いても状態は保持されます。

**メタセル（Note・Directive・Dictionary・Bibliography）および除外設定されたセルは上記の状態機械の外にあります。** これらのセルは展開・翻訳・切替・修正・更新・一括同期の対象にならず、エクスポート時にも本文には出力されません。

---

## 公開 API リファレンス

通常はパレットのボタン操作で十分ですが、スクリプトや自動化のために公開 API を直接呼び出すこともできます。

### 執筆ツール

#### DocExpandIdea

選択したセルのアイデアテキストを LLM を使って文章品質のパラグラフに展開します。
既にパラグラフ展開済みの場合は、現在のパラグラフ文章・Directive セルの指示・文脈を踏まえてパラグラフをインプレースで更新します。

**シグネチャ**

```mathematica
DocExpandIdea[nb, cellIdx]
DocExpandIdea[nb, cellIdx, Fallback -> True]
```

| 引数 | 型 | 説明 |
|---|---|---|
| `nb` | `NotebookObject` | 対象ノートブック |
| `cellIdx` | `Integer` | 対象セルのインデックス（1 始まり） |
| `Fallback` | `True` / `False` | 課金 API（Claude API）を許可するか。デフォルト `False` |

**例**

```mathematica
(* 現在のノートブックの 3 番目のセルを展開 *)
DocExpandIdea[EvaluationNotebook[], 3]

(* 課金 API を許可して展開（既展開セルへの再実行も同様） *)
DocExpandIdea[EvaluationNotebook[], 3, Fallback -> True]
```

---

#### DocToggleView

セルのアイデア表示とパラグラフ表示を切り替えます。
現在表示中の内容（編集済みでも）を保存してから切り替えます。
翻訳済みセルの場合はパラグラフ ↔ 翻訳を、計算モードのセルの場合はプロンプト ↔ コードを切り替えます。

**シグネチャ**

```mathematica
DocToggleView[nb, cellIdx]
```

| 引数 | 型 | 説明 |
|---|---|---|
| `nb` | `NotebookObject` | 対象ノートブック |
| `cellIdx` | `Integer` | 対象セルのインデックス |

**例**

```mathematica
(* 5 番目のセルのアイデア ↔ パラグラフを切り替え *)
DocToggleView[EvaluationNotebook[], 5]
```

---

#### DocTranslate

セルの内容を `$DocTranslationLanguage` で指定した言語に翻訳します。
パラグラフモードのセルが対象です（アイデアモードのセルはスキップされます）。
既に翻訳済みのセルに対して呼び出した場合は、現在のパラグラフから翻訳を再生成します。

**シグネチャ**

```mathematica
DocTranslate[nb, cellIdx]
DocTranslate[nb, cellIdx, Fallback -> True]
```

**例**

```mathematica
(* 2 番目のセルを翻訳（翻訳済みの場合は再翻訳） *)
DocTranslate[EvaluationNotebook[], 2]
```

---

#### DocSync

翻訳済みセルの元テキストと翻訳を再同期します。
元テキストが編集された場合に、翻訳全体を再生成せず差分的に更新します。

> **注意**: パレットには同期ボタンがありません。スクリプトや自動化での利用を想定した API です。軽微な変更には本関数が効率的ですが、大幅な変更の場合は `DocTranslate` で全体を再翻訳することを推奨します。

**シグネチャ**

```mathematica
DocSync[nb, cellIdx]
DocSync[nb, cellIdx, Fallback -> True]
```

**例**

```mathematica
(* 4 番目のセルを同期 *)
DocSync[EvaluationNotebook[], 4]
```

---

#### DocSyncAll

ノートブック内のすべての paragraph / idea セルを、現在の指示（Directive）・辞書（Dictionary）・プロンプトに従って **一括で再生成** します。翻訳付きセルは翻訳側も連鎖的に更新されます。

実行前に件数・推定コスト・推定時間・翻訳表示中セルへの影響を表示する確認ダイアログが出ます。
指示・辞書セルの最終更新時刻より後に編集された対象セルは「既に最新」と判断してスキップします。
処理は非同期チェーンで進行し、フロントエンドをブロックしません。

**シグネチャ**

```mathematica
DocSyncAll[nb]
DocSyncAll[nb, Fallback -> True]
```

| 引数 | 型 | 説明 |
|---|---|---|
| `nb` | `NotebookObject` | 対象ノートブック |
| `Fallback` | `True` / `False` | 課金 API（Claude API）を許可するか。デフォルト `False` |

**例**

```mathematica
(* ノートブック全体に Directive・Dictionary の変更を反映 *)
DocSyncAll[EvaluationNotebook[]]

(* 課金 API を許可して一括同期 *)
DocSyncAll[EvaluationNotebook[], Fallback -> True]
```

---

#### DocCompute

選択したセルのプロンプトテキストから、LLM を使って実行可能な Wolfram Language / Mathematica コードを生成します。
ノートブックのコンテキスト（文脈情報）を自動収集し、略語や固有名詞の意味を文脈から判断します。
生成されたコードはセルに書き込まれ、セルはオレンジ色の枠線の計算モードに切り替わります。
ノートブック内の Directive セルの指示もコード生成時に適用されます。

**シグネチャ**

```mathematica
DocCompute[nb, cellIdx]
DocCompute[nb, cellIdx, Fallback -> True]
```

| 引数 | 型 | 説明 |
|---|---|---|
| `nb` | `NotebookObject` | 対象ノートブック |
| `cellIdx` | `Integer` | 対象セルのインデックス（1 始まり） |
| `Fallback` | `True` / `False` | 課金 API（Claude API）を許可するか。デフォルト `False` |

**例**

```mathematica
(* 現在のノートブックの 3 番目のセルのプロンプトからコードを生成 *)
DocCompute[EvaluationNotebook[], 3]

(* 課金 API を許可してコードを生成 *)
DocCompute[EvaluationNotebook[], 3, Fallback -> True]
```

> **注意**: コード表示中（計算モード）のセルに対して呼び出すことはできません。先に `DocToggleView` でプロンプト表示に切り替えてから実行してください。

---

#### DocRefine

選択したセルのテキストを LLM で **校正（最小修正）** します。

文法的・表現上の不自然さを最小限の修正で訂正し、**元の意味・構成・文体・トーンを完全に保持** します。
Directive セル・Dictionary セルの指示は厳守されます。
翻訳付きセルの場合は翻訳側も連鎖的に更新されます。
**対象外**: idea モードのセル、compute モードのセル、メタセル（Note・Directive・Dictionary・Bibliography）。

**シグネチャ**

```mathematica
DocRefine[nb, cellIdx]
DocRefine[nb, cellIdx, Fallback -> True]
```

| 引数 | 型 | 説明 |
|---|---|---|
| `nb` | `NotebookObject` | 対象ノートブック |
| `cellIdx` | `Integer` | 対象セルのインデックス |
| `Fallback` | `True` / `False` | 課金 API（Claude API）を許可するか。デフォルト `False` |

**例**

```mathematica
(* 3 番目のセルを校正 *)
DocRefine[EvaluationNotebook[], 3]
```

---

#### DocPolish

選択したセルのテキストを LLM で **より完璧に書き直し** ます。

内容（情報・主旨・意図）は保持しつつ、**文の構成や言い回しを必要に応じて大きく変更** します。
段落全体を「ですます調」に書き換えるなど、追加の指示がなくても積極的に整形が行われます。
Directive セル・Dictionary セルの指示は厳守されます。
翻訳付きセルの場合は翻訳側も連鎖的に更新されます。
**対象外**: idea モードのセル、compute モードのセル、メタセル。

**シグネチャ**

```mathematica
DocPolish[nb, cellIdx]
DocPolish[nb, cellIdx, Fallback -> True]
```

| 引数 | 型 | 説明 |
|---|---|---|
| `nb` | `NotebookObject` | 対象ノートブック |
| `cellIdx` | `Integer` | 対象セルのインデックス |
| `Fallback` | `True` / `False` | 課金 API（Claude API）を許可するか。デフォルト `False` |

**例**

```mathematica
(* 3 番目のセルをより完璧に書き直す *)
DocPolish[EvaluationNotebook[], 3]
```

---

#### DocSplitCell

カーソル位置で選択したセルを前半・後半の2つのセルに分割します。

**シグネチャ**

```mathematica
DocSplitCell[nb, cellIdx]
```

| 引数 | 型 | 説明 |
|---|---|---|
| `nb` | `NotebookObject` | 対象ノートブック |
| `cellIdx` | `Integer` | 対象セルのインデックス |

---

#### DocMergeCells

複数のセルを1つのセルに統合します。

**シグネチャ**

```mathematica
DocMergeCells[nb, cellIdxs]
```

| 引数 | 型 | 説明 |
|---|---|---|
| `nb` | `NotebookObject` | 対象ノートブック |
| `cellIdxs` | `List` | 統合するセルのインデックスリスト（順序通りに結合） |

---

#### DocInsertNote

現在のカーソル位置に Note スタイルのセルを挿入します。
Note セルはエクスポート（Markdown・LaTeX・Word）時に本文から除外されます。

**シグネチャ**

```mathematica
DocInsertNote[nb]
```

| 引数 | 型 | 説明 |
|---|---|---|
| `nb` | `NotebookObject` | 挿入先のノートブック |

**例**

```mathematica
(* 現在のカーソル位置に Note セルを挿入 *)
DocInsertNote[EvaluationNotebook[]]
```

ノートブックに `"Note"` スタイルが定義されている場合はそれを使用します。定義がない場合はパッケージ組み込みのカスタム Note セルが挿入されます。

---

#### DocInsertDirective

現在のカーソル位置に Directive スタイルのセルを挿入します。
展開・翻訳・同期・計算・修正・更新の実行時に LLM が順守すべき指示を記載します。

**シグネチャ**

```mathematica
DocInsertDirective[nb]
```

| 引数 | 型 | 説明 |
|---|---|---|
| `nb` | `NotebookObject` | 挿入先のノートブック |

---

#### DocInsertDictionary

現在のカーソル位置に Dictionary スタイルのセルを挿入します。
翻訳時の用語対応を指定するための辞書セルです。

**シグネチャ**

```mathematica
DocInsertDictionary[nb]
```

| 引数 | 型 | 説明 |
|---|---|---|
| `nb` | `NotebookObject` | 挿入先のノートブック |

---

#### DocInsertBibliography

現在のカーソル位置に Bibliography スタイルのセルを挿入します。
参考文献リストを管理します。

**シグネチャ**

```mathematica
DocInsertBibliography[nb]
```

---

#### DocEditFigureMeta

選択した画像セルの図メタデータ（ラベルとキャプション）を編集するダイアログを表示します。

**シグネチャ**

```mathematica
DocEditFigureMeta[nb, cellIdx]
```

| 引数 | 型 | 説明 |
|---|---|---|
| `nb` | `NotebookObject` | 対象ノートブック |
| `cellIdx` | `Integer` | 対象セルのインデックス |

本文中で `<<fig:label>>` と記述するとエクスポート時に自動変換されます。

---

#### DocEditRefSources

選択したセルの依存資料（参照元 PDF）を設定するダイアログを表示します。

**シグネチャ**

```mathematica
DocEditRefSources[nb, cellIdx]
```

---

#### DocAutoInsertCitations

ノートブック内の全セルに自動引用マーカーを挿入します。
依存資料（refSources）から文献リストを構築し、LLM が本文中の適切な位置に `<<cite:key>>` マーカーを挿入します。
Bibliography セルが存在しなければ末尾に自動生成します。

**シグネチャ**

```mathematica
DocAutoInsertCitations[nb]
```

| 引数 | 型 | 説明 |
|---|---|---|
| `nb` | `NotebookObject` | 対象ノートブック |

**例**

```mathematica
DocAutoInsertCitations[EvaluationNotebook[]]
```

---

### エクスポート *(試験的)*

#### DocExportMarkdown

ノートブックを Markdown 形式でエクスポートします。

**シグネチャ**

```mathematica
DocExportMarkdown[nb]
DocExportMarkdown[nb, "MathFormat" -> True]
```

| 引数 | 型 | 説明 |
|---|---|---|
| `nb` | `NotebookObject` | エクスポート対象のノートブック |
| `"MathFormat"` | `True` / `False` | LLM による数式自動フォーマット。デフォルト `False` |

**出力**

- 出力ディレクトリ: `NotebookDirectory[] / <ノートブック名>_md/`
- Note・Directive・Dictionary・Bibliography スタイルのセルおよび除外設定されたセルは出力から除外される
- ラスター画像 → PNG、ベクター画像・計算結果 → PDF で保存
- Input セルはコードブロック（` ```mathematica ``` `）として出力
- 数式は TeX 形式に変換
- `<<fig:label>>` は `{#fig-label width=100%}` 形式の図参照に変換
- `<<cite:key>>` は `[key]` に変換

**例**

```mathematica
DocExportMarkdown[EvaluationNotebook[]]
DocExportMarkdown[EvaluationNotebook[], "MathFormat" -> True]
```

---

#### DocExportLaTeX

ノートブックを LaTeX 形式でエクスポートします。

**シグネチャ**

```mathematica
DocExportLaTeX[nb]
DocExportLaTeX[nb, "MathFormat" -> True]
```

| 引数 | 型 | 説明 |
|---|---|---|
| `nb` | `NotebookObject` | エクスポート対象のノートブック |
| `"MathFormat"` | `True` / `False` | LLM による数式自動フォーマット。デフォルト `False` |

**出力**

- 出力ディレクトリ: `NotebookDirectory[] / <ノートブック名>_LaTeX/`
- Note・Directive・Dictionary・Bibliography スタイルのセルおよび除外設定されたセルは出力から除外される
- 画像は本文幅いっぱい（`\includegraphics[width=\textwidth]`）で出力される
- Input セルはコードブロック環境として出力
- 数式は TeX 形式に変換
- `<<fig:label>>` は `\ref{fig:label}` に、`<<cite:key>>` は `\cite{key}` に変換
- 参考文献は `thebibliography` 環境として末尾に自動出力
- `"MathFormat" -> True` の場合、インライン数式に加えて Box 式・FormBox を含む Mathematica 数式表現も LLM が適切な LaTeX 形式に変換する
- **`"MathFormat"` の値に関わらず常に適用される決定的変換（LLM 不使用）**: 本文中の Unicode 数式記号（添字・上付き文字、ギリシャ文字、集合論・論理記号、矢印、ブラックボード太字 `ℝ ℂ ℤ ℕ ℚ`、アクセント付きラテン文字など）を対応する LaTeX コマンドに変換し、`_ ^ # % &` 等をエスケープする。既存の `$...$` / `\(...\)` 数式や `\cite{}`・`\ref{}`・`\figurename~\ref{}`・`\label{}` は保護され再変換されない。`\cite{}` / `\ref{}` のキーは ASCII に正規化される
- **エンジン自動判別**: 生成される `.tex` は `\RequirePackage{iftex}` を用いて pLaTeX/upLaTeX（`jsarticle` + `dvipdfmx`、Overleaf の和文設定相当）と pdfLaTeX（`article` + `CJKutf8`）のどちらでもコンパイルできるようにプリアンブルを切り替える
- **図の配置**: `[!htbp]` と float パラメータ調整（`\topfraction` 等）により図が本文の参照位置付近にとどまるようにする。図セルの TaggingRules `documentation/figFullPage -> True` を設定すると、その図のみ独立した全ページ図（`[p]`）として出力される

**例**

```mathematica
DocExportLaTeX[EvaluationNotebook[]]
DocExportLaTeX[EvaluationNotebook[], "MathFormat" -> True]
```

---

#### DocExportWord

ノートブックを Microsoft Word (.docx) 形式でエクスポートします。
内部で `DocExportMarkdown` を実行し、Pandoc で `.docx` に変換します。

**シグネチャ**

```mathematica
DocExportWord[nb]
DocExportWord[nb, "MathFormat" -> True]
DocExportWord[nb, "ReferenceDoc" -> "/path/to/template.docx"]
```

| 引数 | 型 | 説明 |
|---|---|---|
| `nb` | `NotebookObject` | エクスポート対象のノートブック |
| `"MathFormat"` | `True` / `False` | LLM による数式自動フォーマット。デフォルト `False` |
| `"ReferenceDoc"` | `String` / `None` | スタイルテンプレートとして使用する `.docx` ファイルのパス。デフォルト `None` |

**出力**

- 出力ファイル: `NotebookDirectory[] / <ノートブック名>_md/<ノートブック名>.docx`
- Pandoc がインストールされていない場合はエラーダイアログを表示してインストール先を案内する

**例**

```mathematica
DocExportWord[EvaluationNotebook[]]
DocExportWord[EvaluationNotebook[], "MathFormat" -> True]
```

---

### パレット

#### ShowDocPalette

ドキュメント作成用パレットを表示します。
パッケージロード時に自動で呼ばれますが、閉じた場合はこの関数で再表示できます。

**シグネチャ**

```mathematica
ShowDocPalette[]
```

**例**

```mathematica
ShowDocPalette[]
```

既にパレットが開いている場合は一度閉じてから再表示します。

---

### 設定変数

#### $DocTranslationLanguage

翻訳先の言語名を指定するグローバル変数です。

- デフォルト: `$Language` が英語以外なら `"English"`、英語なら `"Japanese"`
- ユーザーが任意の言語名に変更可能です

**例**

```mathematica
(* フランス語に翻訳する *)
$DocTranslationLanguage = "French"

(* 日本語に翻訳する *)
$DocTranslationLanguage = "Japanese"
```

---

#### $ClaudeModel（ClaudeCode パッケージ変数）

使用する LLM モデルを指定します。このパッケージの LLM 呼び出しはすべて claudecode パッケージ経由で行われるため、`ClaudeCode`$ClaudeModel` の設定が適用されます。

- **文字列を設定した場合**: Anthropic API を使用します（例: `"claude-sonnet-4-20250514"`）
- **リストを設定した場合**: ローカル LMStudio モデルを使用します（後述の `$ClaudePrivateModel` 参照）

---

#### $ClaudePrivateModel（ClaudeCode パッケージ変数）

LMStudio ローカルモデルの接続設定を `{プロバイダ種別, モデル名, ベースURL}` の 3 要素リストで指定します。

```mathematica
$ClaudePrivateModel = {"lmstudio", "qwen/qwen3.6-27b", "http://127.0.0.1:1234"}
```

| 要素 | 説明 |
|---|---|
| 第1要素 | プロバイダ種別。`"lmstudio"` を指定します |
| 第2要素 | LMStudio で起動しているモデル名 |
| 第3要素 | LMStudio ローカルサーバーのベース URL（デフォルト: `"http://127.0.0.1:1234"`） |

`$ClaudeModel = $ClaudePrivateModel` と設定することでローカルモデルに切り替わります。

---

#### $ClaudeLMStudioIntegrations（ClaudeCode パッケージ変数）

LMStudio モード時に有効化する MCP ツール統合のリストを指定します。

```mathematica
$ClaudeLMStudioIntegrations = {"mcp/exa"}
```

- `"mcp/exa"` を指定すると Exa 検索 MCP が LMStudio モードで利用可能になります
- 空リスト `{}` を指定すると MCP 統合なしで動作します

---

#### 図機能の設定変数（`Documentation`Private`）

清書・図生成・更新（高品質化）の挙動を調整する内部変数です。`Documentation`Private`<変数名> = 値` で設定します。

| 変数 | 既定 | 説明 |
|---|---|---|
| `$iDocCleanFigureModel` | `""` | 清書／図生成で使う Anthropic モデル ID の手動上書き。`""` なら自動判定（`NBGetAvailableFallbackModels` から `claude-…` を選択、既定 `claude-opus-4-6`） |
| `$iDocFigureMaxWidth` | `760` | 生成図の最大表示幅（px）。巨大な図でフロントエンドの整形が重くなるのを防ぐ上限 |
| `$iDocFigureVectorDisplay` | `True` | 図セルの表示形式。`True`（既定）＝ベクター（`Graphics`）でそのまま表示（拡大しても綺麗）。`False`＝ラスター画像で表示（同期のままの画像経路でフリーズが出る場合の保険）。いずれも生成コードは `figCleanCode` タグに保存される |
| `$iDocFigurePolishMethod` | `"vector"` | 更新（図の高品質化）の方式。`"vector"`（Anthropic でベクター強化）／`"gptimage"`（OpenAI gpt-image-1 img2img）／`"dalle"`（OpenAI dall-e-3 text2img） |
| `$iDocPolishColorTheme` | `"auto"` | vector 更新の配色テーマ。`auto`/`pastel`/`monotone`/`vivid`/`blueprint`/`warm`/`cool` |
| `$iDocPolishShadow` | `"subtle"` | vector 更新の影の強さ。`none`/`subtle`/`medium`/`strong` |
| `$iDocPolishRounding` | `"medium"` | vector 更新の角丸。`none`/`small`/`medium`/`large` |
| `$iDocPolishVariants` | `1` | vector 更新の生成案数。`2`〜`4` で複数案を生成しダイアログで選択 |
| `$iDocLastFigError` | `""` | 直近の図処理エラーの全文（失敗時の確認用） |

- 清書・図生成・更新はいずれも **課金 API: 許可** が前提です。`"gptimage"` / `"dalle"` はさらに `SystemCredential["OPENAI_API_KEY"]` が必要です。
- 既定フォントは `ClaudeCode`$ClaudeStandardFont`（既定 `"Yu Gothic UI"`）。図中の日本語もこのフォントで描画されます。

---

## 注意事項

- パレットは `WindowFloating` モードで表示されます。パレットをクリックしても対象ノートブックの選択は変わりません。
- セルを選択せずにボタンを押した場合、直前に操作したセルが再利用されます。
- 複数セルの逐次処理は約 2 秒間隔の非同期チェーンで実行されます。処理中はステータスバーに進捗が表示されます。
- モデル設定は `ClaudeCode` パレットと共有されます。片方で変更すると両方のパレットに反映されます。プロバイダ（P）・モデル（M）・エフォート・課金 API はすべて共通です。
- エクスポート機能（Markdown・LaTeX・Word）は試験的な実装です。数式・複雑な表・埋め込みオブジェクトなど、一部の要素が正確に変換されない場合があります。
- **メタセル（Note・Directive・Dictionary・Bibliography）および除外設定されたセルは、展開・翻訳・同期・修正・更新・一括同期・一括表示切替の対象外であり、Markdown・LaTeX・Word エクスポート時にも本文に含まれません。**
- Directive セルと Dictionary セルはノートブック内に複数配置できます。すべての Directive・Dictionary セルの内容が LLM に渡されます。Directive セルは計算（コード生成）・修正（Refine）・更新（Polish）時にも参照されます。
- `<<fig:label>>` および `<<cite:key>>` マーカーはエクスポート時に自動変換されます。本文中に直接記述してください。
- **×展開・×翻訳・×計算は元に戻せません。** 実行前に確認ダイアログが表示されますが、削除後のデータは復元できないため注意してください。
- Word エクスポートには Pandoc が必要です。インストール方法は `setup.md` を参照してください。
- `DocAutoInsertCitations` は依存資料（Ref Src）が設定されたセルがある場合のみ動作します。先に各セルへ依存資料を設定してください。
- +Math（数式自動フォーマット）オプションは、テキスト中にインラインで書かれた数式表現（例: 「f(x) = x^2」）のほか、Box 式・FormBox を含む Mathematica の数式表現も対象としています。Mathematica の StandardForm 数式セルとは別の処理です。
- **▶ 展開 を展開済みセルに再実行すると、アイデアメモからではなく現在のパラグラフ文章・指示・文脈を踏まえてインプレース更新されます。** Directive や Dictionary を変更した後に既存パラグラフへ反映させたい場合に活用してください。
- **» 翻訳 を翻訳済みセルに再実行すると、現在のパラグラフから翻訳を再生成します。** 元パラグラフの変更が軽微な場合は `DocSync[nb, cellIdx]` API で差分更新できます。
- **計算（コード生成）はコード表示中のセルに再実行できません。** **↔ 切替** でプロンプト表示に戻してからプロンプトを修正し、再度 **計算** を実行してください。生成されたコードはオレンジ色の枠線で表示されます。
- **修正（Refine）と更新（Polish）はセル本文を上書きします。** idea モード・compute モード・メタセルは自動でスキップされます。翻訳付きセルでは翻訳側も連鎖的に更新されます。
- **⇌ 一括同期は強い影響操作です。** ノートブック全体の paragraph / idea セルを再生成し、翻訳がある場合は翻訳も連鎖更新します。実行前の確認ダイアログで件数・コスト・時間を必ず確認してください。指示・辞書セルの最終更新時刻より後に編集されたセルは「既に最新」として自動スキップされます。
- LaTeX エクスポート時の画像は本文幅いっぱいに出力されます（`\includegraphics[width=\textwidth]`）。
- **LaTeX エクスポートは常に決定的な Unicode 数式記号サニタイズ（LLM 不使用）を適用します。** 添字・ギリシャ文字・矢印・ブラックボード太字・アクセント付き文字などが LaTeX コマンドに変換され、既存の `$...$` / `\cite{}` / `\ref{}` / `\label{}` は保護されます。出力される `.tex` は `iftex` によりエンジン（pLaTeX/upLaTeX または pdfLaTeX）を自動判別するため、Overleaf の和文設定でも手元の pdfLaTeX 環境でもそのままコンパイルできます。
- 図は `[!htbp]` と高さ制限で本文参照位置の近くに配置されます。特定の図だけページいっぱいの独立図にしたい場合は、図セルの TaggingRules に `documentation/figFullPage -> True` を設定してください。
- **LMStudio ローカルモデルを使用する場合は、パッケージロード前に LMStudio のローカルサーバーを起動しておいてください。** `$ClaudeModel` をリストに設定している間は、パレットの課金 API 設定や Fallback オプションに関わらず LMStudio が優先されます。
- **プロバイダ（P）ボタン** は claudecode → anthropic → openai → lmstudio を循環します。プロバイダを切り替えるとエフォートは `medium` にリセットされます。
- **モデル（M）ボタン** は現在のプロバイダで利用可能なモデル候補を循環します。候補リストは claudecode パッケージが管理しています。
- **清書（図）・×清書、および図セルに対する修正・更新は、画像対応の課金 API を使用します。** 設定の「課金API: 許可」が必要です。LLM への画像／テキスト送信を伴うため応答まで数十秒かかることがあり、その間カーネルはビジー状態になります（完了後に解放されます）。
- **清書済み（緑枠）のセルに「清書（図）」は再実行できません。** 再生成は **修正**（手書き追記の取り込み）で、または **↔ 切替** で元に戻してから行ってください。
- 生成図は表示幅に上限（`$iDocFigureMaxWidth`、既定 760px）が掛かります。図の更新方式やスタイルは `Documentation`Private`$iDocFigurePolishMethod` ほかの設定変数で切り替えられます（「設定変数 › 図機能の設定変数」参照）。